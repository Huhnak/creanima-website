{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "sass:build": "sass sass/style.scss css/style.css --style compressed",
    "sass:watch": "sass --watch sass/style.scss:css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "svg-inliner:watch": "svg-inliner svg_index.html index.html --watch",
    "dev": "concurrently --kill-others \"npm run sass:watch\" \"npm run svg-inliner:watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "sass": "^1.93.2",
    "@huhnak/svg-inliner": "^1.0.2"
  },
  "dependencies": {
    
  }
}
